---
title: 控件树
description: 层级、排序、多选与键盘导航
---
import { Callout } from 'fumadocs-ui/components/callout';

## 控件树

- **拖拽**：调整层级与顺序（渲染顺序 = 树顺序，靠后的画在上层）。
- **多选**：Shift / Ctrl + 点击。
- **键盘导航**：树获得焦点后，`↑` `↓` 换选、`←` `→` 折叠 / 展开。
- **右键菜单**：复制 / 剪切 / 粘贴 / 重命名 / 删除 / 新建子控件。


