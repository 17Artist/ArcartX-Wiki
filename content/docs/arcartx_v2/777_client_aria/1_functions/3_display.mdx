---
title: Display:窗口显示
description: Display窗口控制工具集
---
import { Callout } from 'fumadocs-ui/components/callout';
import { Step, Steps } from 'fumadocs-ui/components/steps';
import { Card, Cards } from 'fumadocs-ui/components/card';

## `Display.setResizable(resizable){:aria}`
- 设置窗口是否可调整大小

<Callout type="info">
- **参数**：`resizable`: 布尔类型
- **返回值**：无
- **调用示例**
```aria
Display.setResizable(true)
```
</Callout>

## `Display.setFullScreen(fullscreen){:aria}`
- 设置窗口是否全屏

<Callout type="info">
- **参数**：`fullscreen`: 布尔类型
- **返回值**：无
- **调用示例**
```aria
Display.setFullScreen(true)
```
</Callout>

## `Display.isFullScreen(){:aria}`
- 获取窗口是否处于全屏状态

<Callout type="info">
- **参数**：无
- **返回值**：布尔类型
- **调用示例**
```aria
Display.isFullScreen()
```
- **示例返回值**
```aria
true
```
</Callout>

## `Display.isResizable(){:aria}`
- 获取窗口是否可调整大小

<Callout type="info">
- **参数**：无
- **返回值**：布尔类型
- **调用示例**
```aria
Display.isResizable()
```
- **示例返回值**
```aria
true
```
</Callout>

## `Display.desktopWidth(){:aria}`
- 获取桌面宽度

<Callout type="info">
- **参数**：无
- **返回值**：整数类型
- **调用示例**
```aria
Display.desktopWidth()
```
- **示例返回值**
```aria
1920
```
</Callout>

## `Display.desktopHeight(){:aria}`
- 获取桌面高度

<Callout type="info">
- **参数**：无
- **返回值**：整数类型
- **调用示例**
```aria
Display.desktopHeight()
```
- **示例返回值**
```aria
1080
```
</Callout>

## `Display.width(){:aria}`
- 获取窗口宽度

<Callout type="info">
- **参数**：无
- **返回值**：整数类型
- **调用示例**
```aria
Display.width()
```
- **示例返回值**
```aria
1280
```
</Callout>

## `Display.height(){:aria}`
- 获取窗口高度

<Callout type="info">
- **参数**：无
- **返回值**：整数类型
- **调用示例**
```aria
Display.height()
```
- **示例返回值**
```aria
720
```
</Callout>

## `Display.x(){:aria}`
- 获取窗口X坐标

<Callout type="info">
- **参数**：无
- **返回值**：整数类型
- **调用示例**
```aria
Display.x()
```
- **示例返回值**
```aria
100
```
</Callout>

## `Display.y(){:aria}`
- 获取窗口Y坐标

<Callout type="info">
- **参数**：无
- **返回值**：整数类型
- **调用示例**
```aria
Display.y()
```
- **示例返回值**
```aria
100
```
</Callout>

## `Display.setLocation(x, y){:aria}`
- 设置窗口位置

<Callout type="info">
- **参数**：
    - `x`: 整数类型
    - `y`: 整数类型
- **返回值**：无
- **调用示例**
```aria
Display.setLocation(100, 100)
```
</Callout>

## `Display.resize(width, height){:aria}`
- 调整窗口大小

<Callout type="info">
- **参数**：
    - `width`: 整数类型
    - `height`: 整数类型
- **返回值**：无
- **调用示例**
```aria
Display.resize(1280, 720)
```
</Callout>

## `Display.getScale(){:aria}`
- 获取窗口缩放比例

<Callout type="info">
- **参数**：无
- **返回值**：浮点类型
- **调用示例**
```aria
Display.getScale()
```
- **示例返回值**
```aria
1.0
```
</Callout>