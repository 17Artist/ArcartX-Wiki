---
title: Mouse:鼠标
description: Mouse鼠标工具集
---
import { Callout } from 'fumadocs-ui/components/callout';
import { Step, Steps } from 'fumadocs-ui/components/steps';
import { Card, Cards } from 'fumadocs-ui/components/card';

## `Mouse.wheel(){:aria}`
- 获取鼠标滚轮滚动值

<Callout type="info">
    - **参数**：无
    - **返回值**：双精度浮点数类型
    - **调用示例**
    ```aria
    Mouse.wheel()
    ```
</Callout>

## `Mouse.x(){:aria}`
- 获取鼠标X坐标

<Callout type="info">
    - **参数**：无
    - **返回值**：双精度浮点数类型
    - **调用示例**
    ```aria
    Mouse.x()
    ```
</Callout>

## `Mouse.y(){:aria}`
- 获取鼠标Y坐标

<Callout type="info">
    - **参数**：无
    - **返回值**：双精度浮点数类型
    - **调用示例**
    ```aria
    Mouse.y()
    ```
</Callout>

## `Mouse.getEventButton(){:aria}`
- 获取鼠标当前按下的按键

<Callout type="info">
    - **参数**：无
    - **返回值**：整数类型
    - **调用示例**
    ```aria
    Mouse.getEventButton()
    ```
</Callout>

