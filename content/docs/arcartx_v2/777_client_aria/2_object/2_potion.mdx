---
title: PotionEffect:药水效果
description: PotionEffect:药水效果
---
import { Callout } from 'fumadocs-ui/components/callout';
import { Step, Steps } from 'fumadocs-ui/components/steps';
import { Card, Cards } from 'fumadocs-ui/components/card';

## `getCategory(){:aria}`
- 获取药水效果分类
<Callout type="info">
    - **参数**：无
    - **返回值**：字符串类型
    - **调用示例**
    ```aria
    药水效果对象.getCategory()
    ```
</Callout>

## `getDisplayName(){:aria}`
- 获取药水效果显示名称
<Callout type="info">
    - **参数**：无
    - **返回值**：字符串类型
    - **调用示例**
    ```aria
    药水效果对象.getDisplayName()
    ```
</Callout>

## `getDuration(){:aria}`
- 获取药水效果持续时间
<Callout type="info">
    - **参数**：无
    - **返回值**：整数类型
    - **调用示例**
    ```aria
    药水效果对象.getDuration()
    ```
</Callout>

## `getLevel(){:aria}`
- 获取药水效果等级
<Callout type="info">
    - **参数**：无
    - **返回值**：整数类型
    - **调用示例**
    ```aria
    药水效果对象.getLevel()
    ```
</Callout>

## `getTime(){:aria}`
- 获取药水效果持续时间（秒）
<Callout type="info">
    - **参数**：无
    - **返回值**：整数类型
    - **调用示例**
    ```aria
    药水效果对象.getTime()
    ```
</Callout>
