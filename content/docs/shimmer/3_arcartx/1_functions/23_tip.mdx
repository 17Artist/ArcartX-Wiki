---
title: Tip:工具提示
description: Tip工具集
---
import { Callout } from 'fumadocs-ui/components/callout';
import { Step, Steps } from 'fumadocs-ui/components/steps';
import { Card, Cards } from 'fumadocs-ui/components/card';

## `Tip.getText(){:Shimmer}`
- 获取Tip文本

<Callout type="info">
    - **参数**：无
    - **调用示例**
    ```Shimmer
    Tip.getText()
    ```
    - **返回值**：列表型——包含Tip文本的字符串列表，解析的文本会将!&!* !&!# !&!%替换为§* §# §%
</Callout>

## `Tip.getDisplay(){:Shimmer}`
- 获取物品显示名（第一行Tip文本）
<Callout type="info">
    - **参数**：无
    - **调用示例**
    ```Shimmer
    Tip.getDisplay()
    ```
    - **返回值**：字符串型——物品显示名（第一行Tip文本），解析的文本会将!&!* !&!# !&!%替换为§* §# §%
</Callout>

## `Tip.getLore(){:Shimmer}`
- 获取物品Lore（除第一行外的Tip文本）
<Callout type="info">
    - **参数**：无
    - **调用示例**
    ```Shimmer
    Tip.getLore()
    ```
    - **返回值**：列表型——包含物品Lore的字符串列表，解析的文本会将!&!* !&!# !&!%替换为§* §# §%
</Callout>

## `Tip.getItemStack(){:Shimmer}`
- 获取当前Tip的物品对象
<Callout type="info">
    - **参数**：无
    - **调用示例**
    ```Shimmer
    Tip.getItemStack()
    ```
    - **返回值**：ItemStack型——当前Tip的物品对象
</Callout>

