---
title: 文本类型函数
description: 文本类型函数表
---
import { Callout } from 'fumadocs-ui/components/callout';
import { Step, Steps } from 'fumadocs-ui/components/steps';
import { Card, Cards } from 'fumadocs-ui/components/card';

### `contains(otherText){:Shimmer}`
- 判断目标文本是否包含指定内容

<Callout type="info">
- **参数**：`otherText`：文本类型
- **返回值**：布尔类型
- **调用示例**
```Shimmer
'一段文本'.contains('文本')
```
- **示例返回值**
```Shimmer
true
```
</Callout>

### `equals(otherText){:Shimmer}`
- 判断目标文本是否等于指定内容

<Callout type="info">
- **参数**：`otherText`：文本类型
- **返回值**：布尔类型
- **调用示例**
```Shimmer
'一段文本'.equals('一段文本')
```
- **示例返回值**
```Shimmer
true
```
</Callout>

### `equalsIgnoreCase(otherText){:Shimmer}`
- 判断目标文本是否在忽略大小写的情况下等于指定内容

<Callout type="info">
- **参数**：`otherText`：文本类型
- **返回值**：布尔类型
- **调用示例**
```Shimmer
'一段文本'.equalsIgnoreCase('一段文本')
```
- **示例返回值**
```Shimmer
true
```
</Callout>

### `endsWith(suffix){:Shimmer}`
- 判断目标文本是否以指定后缀结尾

<Callout type="info">
- **参数**：`suffix`：文本类型
- **返回值**：布尔类型
- **调用示例**
```Shimmer
'一段文本'.endsWith('文本')
```
- **示例返回值**
```Shimmer
true
```
</Callout>

### `startsWith(prefix){:Shimmer}`
- 判断目标文本是否以指定前缀开头

<Callout type="info">
- **参数**：`prefix`：文本类型
- **返回值**：布尔类型
- **调用示例**
```Shimmer
'一段文本'.startsWith('一段')
```
- **示例返回值**
```Shimmer
true
```
</Callout>

### `startsWith(prefix, toffset){:Shimmer}`
- 判断目标文本是否从指定索引开始以指定前缀开头

<Callout type="info">
- **参数**：`prefix`：文本类型, `toffset`：整数类型
- **返回值**：布尔类型
- **调用示例**
```Shimmer
'一段文本'.startsWith('段', 1)
```
- **示例返回值**
```Shimmer
true
```
</Callout>

### `toLowerCase(){:Shimmer}`
- 将目标文本转换为小写

<Callout type="info">
- **参数**：无
- **返回值**：文本类型
- **调用示例**
```Shimmer
'一段文本'.toLowerCase()
```
- **示例返回值**
```Shimmer
'一段文本'
```
</Callout>

### `toUpperCase(){:Shimmer}`
- 将目标文本转换为大写

<Callout type="info">
- **参数**：无
- **返回值**：文本类型
- **调用示例**
```Shimmer
'一段文本'.toUpperCase()
```
- **示例返回值**
```Shimmer
'一段文本'
```
</Callout>

### `replace(target, replacement){:Shimmer}`
- 将目标文本中的指定内容替换为新的内容

<Callout type="info">
- **参数**：`target`：文本类型, `replacement`：文本类型
- **返回值**：文本类型
- **调用示例**
```Shimmer
'一段文本'.replace('文本', '内容')
```
- **示例返回值**
```Shimmer
'一段内容'
```
</Callout>

### `replaceAll(regex, replacement){:Shimmer}`
- 使用正则表达式将目标文本中的指定内容替换为新的内容

<Callout type="info">
- **参数**：`regex`：文本类型, `replacement`：文本类型
- **返回值**：文本类型
- **调用示例**
```Shimmer
'一段文本'.replaceAll('文本', '内容')
```
- **示例返回值**
```Shimmer
'一段内容'
```
</Callout>

### `replaceFirst(regex, replacement){:Shimmer}`
- 使用正则表达式将目标文本中的第一个匹配项替换为新的内容

<Callout type="info">
- **参数**：`regex`：文本类型, `replacement`：文本类型
- **返回值**：文本类型
- **调用示例**
```Shimmer
'一段文本'.replaceFirst('文本', '内容')
```
- **示例返回值**
```Shimmer
'一段内容'
```
</Callout>

### `split(regex){:Shimmer}`
- 使用正则表达式将目标文本拆分为多个部分

<Callout type="info">
- **参数**：`regex`：文本类型
- **返回值**：列表类型
- **调用示例**
```Shimmer
'一段文本'.split('段')
```
- **示例返回值**
```Shimmer
['一', '文本']
```
</Callout>

### `split(regex, limit){:Shimmer}`
- 使用正则表达式将目标文本拆分为多个部分，最多拆分为指定数量的部分

<Callout type="info">
- **参数**：`regex`：文本类型, `limit`：整数类型
- **返回值**：列表类型
- **调用示例**
```Shimmer
'一段文本'.split('段', 2)
```
- **示例返回值**
```Shimmer
['一', '文本']
```
</Callout>

### `length(){:Shimmer}`
- 获取目标文本的长度

<Callout type="info">
- **参数**：无
- **返回值**：整数类型
- **调用示例**
```Shimmer
'一段文本'.length()
```
- **示例返回值**
```Shimmer
4
```
</Callout>

### `isEmpty(){:Shimmer}`
- 判断目标文本是否为空

<Callout type="info">
- **参数**：无
- **返回值**：布尔类型
- **调用示例**
```Shimmer
''.isEmpty()
```
- **示例返回值**
```Shimmer
true
```
</Callout>

### `substring(beginIndex){:Shimmer}`
- 获取目标文本的子字符串，从指定索引开始

<Callout type="info">
- **参数**：`beginIndex`：整数类型
- **返回值**：文本类型
- **调用示例**
```Shimmer
'一段文本'.substring(1)
```
- **示例返回值**
```Shimmer
'段文本'
```
</Callout>

### `substring(beginIndex, endIndex){:Shimmer}`
- 获取目标文本的子字符串，从指定开始索引到结束索引

<Callout type="info">
- **参数**：`beginIndex`：整数类型, `endIndex`：整数类型
- **返回值**：文本类型
- **调用示例**
```Shimmer
'一段文本'.substring(1, 3)
```
- **示例返回值**
```Shimmer
'段文'
```
</Callout>

### `indexOf(substring){:Shimmer}`
- 获取目标文本中指定子字符串的索引

<Callout type="info">
- **参数**：`substring`：文本类型
- **返回值**：整数类型
- **调用示例**
```Shimmer
'一段文本'.indexOf('文本')
```
- **示例返回值**
```Shimmer
2
```
</Callout>

### `indexOf(substring, fromIndex){:Shimmer}`
- 获取目标文本中指定子字符串的索引，从指定索引开始

<Callout type="info">
- **参数**：`substring`：文本类型, `fromIndex`：整数类型
- **返回值**：整数类型
- **调用示例**
```Shimmer
'一段文本'.indexOf('文本', 1)
```
- **示例返回值**
```Shimmer
2
```
</Callout>

### `lastIndexOf(substring){:Shimmer}`
- 获取目标文本中指定子字符串的最后一个索引

<Callout type="info">
- **参数**：`substring`：文本类型
- **返回值**：整数类型
- **调用示例**
```Shimmer
'一段文本'.lastIndexOf('文本')
```
- **示例返回值**
```Shimmer
2
```
</Callout>

### `lastIndexOf(substring, fromIndex){:Shimmer}`
- 获取目标文本中指定子字符串的最后一个索引，从指定索引开始

<Callout type="info">
- **参数**：`substring`：文本类型, `fromIndex`：整数类型
- **返回值**：整数类型
- **调用示例**
```Shimmer
'一段文本'.lastIndexOf('文本', 1)
```
- **示例返回值**
```Shimmer
-1
```
</Callout>